import * as React from "react";
import { Field, SearchBox } from "@fluentui/react-components";

export const SearchBoxExp = () => {
  return (
    <Field label="Search">
      <SearchBox />
    </Field>
  );
};